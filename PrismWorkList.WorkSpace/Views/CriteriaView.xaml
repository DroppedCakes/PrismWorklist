<UserControl
    x:Class="PrismWorkList.WorkSpace.Views.CriteriaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:views="clr-namespace:PrismWorkList.WorkSpace.Views"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Horizontal">
            <DatePicker SelectedDate="{Binding Path=StudyDateSince}" SelectedDateFormat="Long" />
            <TextBlock Text=" ～ " />
            <DatePicker SelectedDate="{Binding Path=StudyDateUntil}" SelectedDateFormat="Long" />
        </StackPanel>

        <StackPanel Grid.Column="1">
            <Button Command="{Binding Path=StudiesReloadCommand}" Content="検索" />
        </StackPanel>
    </Grid>

</UserControl>
